# Miokr · 纯前端本地 CSV 数据可视化工具

> miokr.com ｜ 面向仿真工程师、CAE / 有限元、科研人员与数据分析者的轻量级可视化工具。
> **所有文件全程在浏览器本地处理，不上传任何服务器。**

Miokr 是一款零后端、零数据库、可直接双击打开使用的静态可视化工具站。基于 D3.js 与 Observable Plot（均为 ISC 开源协议）构建，支持从基础统计图到高级可视化的完整图表能力，导出结果可用于论文、工程报告与商业演示。

---

## ✨ 核心特性

- **本地处理 · 零上传**：CSV 仅在浏览器内解析运算，数据不出本机，无隐私收集、无后台存储。
- **图表全覆盖**：10 种图表类型，一键切换。
- **自动列识别**：自动区分数值列 / 文本列，并智能识别经纬度列。
- **手动选列 · 实时刷新**：自由选择 X / Y / 分组等字段，图表即时更新。
- **高清导出**：一键导出 PNG / SVG；会员无水印、高清输出。
- **合规安全**：地图功能仅渲染经纬度散点，不含任何底图，规避测绘资质问题。
- **双击即用**：依赖库本地内置，示例数据内联，`file://` 直接打开即可运行。

---

## 📊 支持的图表类型

| 类别 | 图表 | 所需字段 |
| --- | --- | --- |
| 基础 | 散点图 | X(数值)、Y(数值)、分组(可选) |
| 基础 | 折线图 | X(数值)、Y(数值) |
| 基础 | 面积图 | X(数值)、Y(数值) |
| 基础 | 柱状图 | 分类 X |
| 基础 | 直方图 | 数值列 |
| 基础 | 箱线图 | 分组(可选)、数值 Y |
| 高级 | 2D 网格热力图 | X(数值)、Y(数值)、数值 |
| 高级 | 桑基流量图 | 源、目标、数值 |
| 高级 | 拓扑网络图（可拖拽 + 力学模拟） | 源、目标、权重(可选) |
| 高级 | 经纬度点位图（无底图） | 经度、纬度、名称(可选)、数值(可选) |

所有图表均自带 tooltip 悬浮信息。

---

## 🚀 使用方式

### 方式一：直接打开（最简单）

双击仓库根目录的 `index.html` 即可在浏览器中运行，无需安装、无需联网、无需服务器。

### 方式二：本地静态服务器（推荐用于开发调试）

```bash
# Python
python -m http.server 8080

# 或 Node
npx serve .
```

然后访问 `http://localhost:8080`。

### 方式三：部署上线

本项目为纯静态站，可直接部署到任意静态托管：

- **GitHub Pages**：仓库 Settings → Pages → 选择 `main` 分支根目录即可。
- **任意 Web 服务器 / 对象存储 / CDN**：上传全部文件，保持目录结构不变。

---

## 🖱️ 工具页操作流程

1. 顶部「数据源」区：上传本地 CSV，或从下拉选择内置示例数据。
2. 「图表类型」区：点击切换 10 种图表之一。
3. 「字段映射」区：为当前图表选择对应的数据列（切换图表类型会自动预选合适字段）。
4. 右侧图表区实时刷新，悬浮查看 tooltip。
5. 右上角「导出 SVG / 导出 PNG」下载图表。

---

## 📁 目录结构

```
miokr-visualizer/
├── index.html            # 首页
├── tool.html             # 工具主页面（核心）
├── pricing.html          # 定价 / 会员说明
├── assets/
│   ├── css/              # base / landing / tool / pricing 样式
│   └── js/
│       ├── lib/          # 本地内置依赖：d3 / plot / d3-sankey
│       ├── data/samples.js  # 内联示例数据
│       ├── csv.js        # CSV 解析 + 列类型识别
│       ├── charts.js     # 基础图表 + 热力图
│       ├── sankey.js     # 桑基流量图
│       ├── network.js    # 拓扑网络图
│       ├── geo.js        # 经纬度点位图
│       ├── export.js     # PNG / SVG 导出 + 水印
│       └── app.js        # 工具页主控
├── samples/              # 独立示例 CSV（可下载测试）
├── scripts/gen-samples.js# 示例数据生成脚本（构建辅助）
├── LICENSE               # ISC
└── README.md
```

---

## 💳 免费版与会员版

| 权益 | 免费版 | 会员版 |
| --- | --- | --- |
| 全部 10 种图表 | ✅ | ✅ |
| 本地 CSV 上传解析 | ✅ | ✅ |
| 自动列识别 | ✅ | ✅ |
| PNG / SVG 导出 | ✅（带水印） | ✅（无水印） |
| 高清 2x/3x 输出 | — | ✅ |
| 商用授权 | — | ✅ |

会员采用**一次性买断**，非订阅制。MVP 阶段为零后端产品，会员状态保存在本机浏览器 `localStorage`，不涉及任何账号体系与服务器存储。可在「定价」页开启体验会员模式。

---

## 🔒 合规声明

- 所有用户文件（CSV 等）**100% 在浏览器本地解析**，绝不上传后端 / 服务器。
- 不抓取、不调用、不存储任何境外实时数据。
- 不做自主地图底图；地图功能仅以 D3 投影渲染经纬度散点，规避国家测绘资质问题。
- 全站无任何用户隐私收集、无后台数据存储（MVP 零后端）。
- 所有示例数据、样例 CSV 均为**自制原创**，无版权问题。

页脚固定标注：

> 本工具基于 D3.js、Observable Plot（ISC 开源协议）开发
> 所有用户文件仅在浏览器本地处理，不会上传服务器
> Miokr® 可视化工具 版权所有

---

## 📄 开源协议与致谢

本项目基于以下 ISC 协议开源库构建：

- [D3.js](https://d3js.org/) v7 — 数据可视化底层库（ISC）
- [Observable Plot](https://github.com/observablehq/plot) v0.6 — 图表语法库（ISC）
- [d3-sankey](https://github.com/d3/d3-sankey) v0.12 — 桑基图布局（ISC）

本项目源码同样以 **ISC 协议** 发布，详见 [LICENSE](./LICENSE)。

---

**Miokr® 可视化工具 版权所有 © 2026**
