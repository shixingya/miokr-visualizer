# 更新日志

Miokr 采用「每版本一次迭代」的节奏，版本号形如 `v1.x.0`，每次迭代完成后打 tag 并推送。

---

## [v1.1.0] — 2026-09-25

### 新增功能

- **数据清洗与筛选面板**：位于工具页「数据源」与「图表类型」之间。
  - 缺失值处理：删除含缺失值的行 / 填充均值（仅数值列）/ 填充常数；可按单列或全部列执行。
  - 行去重：按指定列或全部列比对，保留首次出现的行。
  - 条件筛选：范围（≥ / ≤，可只填一边）、包含、等于、不等于、大于、小于。
  - 行排序：任意列升序 / 降序；数值列按数值比较，文本列按中文排序，空值始终排在末尾。
- **非破坏式规则管线**：所有规则以有序清单展示，可逐条删除或一键清空；任何改动都基于原始数据重新计算，原始数据永不被修改。
- **可追溯提示**：数据栏显示「原始 N 行 / 清洗后 M 行」，图表标题显示已应用规则数，每条规则标注「处理前 → 处理后」行数。
- **空结果保护**：清洗后无剩余数据时显示中文空态提示，并阻止导出。
- **新增内置示例数据** `clean_demo`（含缺失与重复的试验记录），内置示例共 7 组。
- 输入校验：文本列不支持填充均值时给出中文提示并阻止添加规则；范围筛选要求至少填写一个边界。

### 修复内容

- 本版本为首次迭代发布，无历史缺陷修复。

---
